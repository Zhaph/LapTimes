@model IEnumerable<LapTimes.Models.Car>

@{
  ViewBag.Title = "Manage Cars";
}

<hgroup class="title">
  <h1>@ViewBag.Title</h1>
</hgroup>

<p>
    @Html.ActionLink("Create a new car", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
          @Html.ActionLink(item.Name, "Details", new { id = item.CarId })
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CarId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CarId })
        </td>
    </tr>
}

</table>
